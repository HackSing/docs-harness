迁移流程要求先执行 `python3 scripts/harness.py project upgrade --target <project> --json` 预览变更，确认 `write_performed=false` 后再执行 `--apply` 模式；升级后使用 `project diff` 与 `project check` 验证 `empty_rules_legal=true`、`legacy_rules_required=false` 等完成标准。