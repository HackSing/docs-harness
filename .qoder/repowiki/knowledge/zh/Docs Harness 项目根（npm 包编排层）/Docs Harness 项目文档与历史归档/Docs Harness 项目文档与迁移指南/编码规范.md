- 每个子目录均提供 README 说明其职责边界，明确区分“当前真源”与“历史只读档案”，禁止将历史方案当作当前实现依据。
- 迁移与升级类文档采用“目标 → 原则 → 步骤 → 清理规则 → 失败关闭条件 → 验收标准”的固定章节结构。
- 所有可执行操作均以完整 `python3 scripts/harness.py ... --json` 命令形式给出，便于机器解析与自动化校验。
- 对受管文件与旧工件的增删改必须基于指纹或 Schema 归属判断，禁止猜测或覆盖归属不明的项目内容。