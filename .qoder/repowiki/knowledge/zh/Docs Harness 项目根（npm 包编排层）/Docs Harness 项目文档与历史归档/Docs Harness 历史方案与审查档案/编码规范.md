- 方案文档统一采用“背景与目标 → 设计要点分节 → 残留与预期对齐 → 合并必改清单 → 验收标准 → 安全边界 → 预期效果”的结构化章节组织。
- 对运行时故障原因码（如 `write_scope_violation`、`missing_evidence_types`、`concurrent_drift_overlap`）进行分级承诺标注（控制器可消灭 / 可消灭 / 一次重准入即过 / 零试探至多一轮），并在实现中逐条映射到具体分支与载荷。
- 审查报告以“总体结论（通过/阻塞）+ P0 安全关切逐项核查 + P1 行为缺陷 + P2 附带问题 + 已验证无问题关键路径清单”的固定模板输出，便于快速定位风险等级。
- 方案与审查中所有断言均以测试编号（T1–T17）或验收阶段（V1–V6）索引，形成“需求 → 测试 → 验收”的可追溯闭环。
- 新增响应字段与只读 action（如 `evidence_checklist`、`pending_context_receipts`、`changes-preview`）严格遵循“不改判定逻辑、零状态变更”的约束，并通过 state_lock 临界区保护写路径。