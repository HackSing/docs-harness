- 所有对外命令统一通过 `python3 scripts/harness.py <subcommand> --target . --json` 调用，响应格式固定为 JSON。
- 版本信息由独立的 VERSION 文件与 package.json 的 version 字段共同维护，SKILL.md metadata.version 与之保持一致。
- 子模块通过 package.json 的 files 数组显式声明纳入发布的目录与文件，未列出的内容不参与打包。