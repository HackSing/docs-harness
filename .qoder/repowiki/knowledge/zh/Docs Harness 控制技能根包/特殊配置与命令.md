根级 npm scripts 提供统一入口：`npm test` 运行 tests 下的 unittest 用例，`npm run self-test` 调用 `scripts/harness.py self-test --target . --json` 进行自测，`npm run pack:check` 执行 `npm pack --dry-run` 校验发布包。项目安装通过 `python3 scripts/harness.py project init --target <project> --json` 完成。