- 所有版本相关变更先在 `docs/plans/` 下建立独立方案文档，再通过相对链接在 `todo.md` 中引用，禁止直接复制方案正文到 TODO。
- 契约文件使用 JSON 代码块 + 表格形式描述字段、枚举与退出码，保持 schema_version 与版本号（如 v1.6.6、v2）显式声明。
- 文档中的事实来源统一在末尾以「事实来源」小节列出具体文件名（如 `scripts/harness.py`、`package.json`、`tests/test_harness.py`）。
- 计划与审查文档按 `vX.Y[.Z]-<主题>-plan.md` / `<主题>-claude-code-review.md` 命名约定组织，便于按版本检索。